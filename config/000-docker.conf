<VirtualHost *:80>
  Use vhost ${VIRTUAL_HOST} ${APACHE_SERVERADMIN} ${PHP_FPM_HOST} ${PHP_FPM_PORT}
  Use vhost_deflate
</VirtualHost>
# <VirtualHost *:443>
#   Use vhost ${VIRTUAL_HOST} ${APACHE_EMAIL} ${PHP_FPM_HOST} ${PHP_FPM_PORT}
#   Use vhost_ssl ${VIRTUAL_HOST}
#   Use vhost_deflate
# </VirtualHost>
